<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/height_app_bar_layout"
        android:background="@color/app_bar_layout"
        android:fitsSystemWindows="true">

        <android.support.design.widget.CollapsingToolbarLayout
            android:id="@+id/collapsingToolbar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fitsSystemWindows="true"
            app:contentScrim="@color/collapsing_toolbar"
            app:expandedTitleGravity="center_horizontal|bottom"
            app:expandedTitleMarginEnd="@dimen/title_margin_end_collapsing_toolbar"
            app:expandedTitleMarginStart="@dimen/title_margin_start_collapsing_toolbar"
            app:layout_scrollFlags="scroll|exitUntilCollapsed"
            app:title="@string/collapsing_toolbar_title">

            <!--            <ImageView
                            android:id="@+id/collapsingImage"
                            android:layout_width="match_parent"
                            android:layout_height="match_parent"
                            android:fitsSystemWindows="true"
                            android:scaleType="centerCrop"
                            app:layout_collapseMode="parallax" />-->

            <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/toolbar"
                app:layout_collapseMode="pin"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
                app:titleEnabled="false"
                app:titleMarginEnd="@dimen/title_margin_end_toolbar"
                app:titleMarginStart="@dimen/title_margin_start_toolbar"
                app:titleTextColor="@color/yellow_700">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical|end"
                    android:drawableStart="@drawable/ic_baseline_shopping_cart_24px"
                    android:drawablePadding="@dimen/drawable_padding_text_toolbar"
                    android:paddingStart="@dimen/padding_start_text_toolbar"
                    android:paddingEnd="@dimen/padding_end_text_toolbar"
                    android:text="@string/start_text_toolbar"
                    android:textAppearance="@style/TextAppearance.MaterialComponents.Headline5"
                    android:textColor="@color/toolbar_text"
                    android:textStyle="bold" />

            </android.support.v7.widget.Toolbar>
        </android.support.design.widget.CollapsingToolbarLayout>
    </android.support.design.widget.AppBarLayout>

    <android.support.v4.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/nested_scroll_view"
        android:scrollbarDefaultDelayBeforeFade="@integer/scrollbar_default_delay_before_fade_nested_scroll_view"
        android:scrollbarSize="@dimen/scrollbar_size_nested_scroll_view"
        android:scrollbars="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <FrameLayout
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        </FrameLayout>

    </android.support.v4.widget.NestedScrollView>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/margin_fab"
        android:elevation="@dimen/elevation_fab"
        android:src="@drawable/ic_baseline_shopping_cart_24px"
        android:textAppearance="@style/TextAppearance.MaterialComponents.Headline5"
        app:backgroundTint="@color/fab_background_tint" />
</android.support.design.widget.CoordinatorLayout>